@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "logoutButton" }))
    {
    @Html.AntiForgeryToken()
             @*@Html.ActionLink(User.Identity.GetUserName(), "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", @class = "loginButton"  })*@
        @Html.ActionLink(VRLibrary.Controllers.AccountController.UserName(User.Identity), "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", @class = "loginButton" })
            <a class="logoutButton" href="javascript:document.getElementById('logoutForm').submit()" >Log off</a>
    }
}
else
{

        @Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink", @class = "loginButton" })
        @Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink", @class = "loginButton" })
   
}
